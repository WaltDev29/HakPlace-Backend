name: hakplace

services:
  db:
    image: mysql:8.0
    container_name: hakplace-db
    restart: always
    environment:
      - TZ=Asia/Seoul
      - LANG=C.UTF-8
      - LC_ALL=C.UTF-8
    env_file:
      - ../env/.env.db
    volumes:
      - ../../services/db:/var/lib/mysql

  api:
    build:
      context: ../../services/api
      dockerfile: Dockerfile
    container_name: hakplace-api
    restart: always
    environment:
      - TZ=Asia/Seoul
    env_file:
      - ../env/.env.api
    ports:
      - "8000:8000"
    volumes:
      - ../../services/api/static:/app/static
    depends_on:
      - db
